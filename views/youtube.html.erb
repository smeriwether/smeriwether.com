<header>
  <a href="index.html">Go back home</a>

  <h1 class="mt-4 md:mt-8">Youtube videos I've created</h1>
  <p class="text-gray-600">
    I recently started recording and uploading videos to Youtube. If I publish something new,
    it'll also appear here. <a href="" target="_blank">Head over to my channel to subscribe</a>.
  </p>
</header>

<ul class="ml-0">
  <% @youtube_videos.each do |video| %>
    <li class="flex flex-col mt-8 md:mt-12">
      <%== video.content_html %>
      <a href="<%= video.url %>" target="_blank" class="mt-4"><%= video.title %></a>
      <span class="mt-2"><%= video.description %></span>
    </li>
  <% end %>
</ul>
